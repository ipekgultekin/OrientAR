package com.example.orientar.navigation.util

import org.junit.Test
import org.junit.Assert.assertEquals

/**
 * Trivial smoke test to verify JVM unit test infrastructure is functional.
 * Confirms: Kotlin compiles, JUnit 4 runs, sub-package layout works.
 */
class SmokeTest {

    @Test
    fun `2 plus 2 equals 4`() {
        assertEquals(4, 2 + 2)
    }
}
