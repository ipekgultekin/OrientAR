<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:paddingTop="12dp">

    <!-- Drag handle -->
    <View
        android:layout_width="32dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="14dp"
        android:background="@drawable/bg_campus_handle" />

    <!-- Title -->
    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select destination"
        android:textColor="@color/campus_text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="12dp" />

    <!-- Search bar -->
    <EditText
        android:id="@+id/searchEditText"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_campus_search_field"
        android:hint="Search..."
        android:textColorHint="@color/campus_placeholder"
        android:textColor="@color/campus_text_primary"
        android:textSize="14sp"
        android:singleLine="true"
        android:imeOptions="actionSearch"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:drawableStart="@drawable/ic_campus_search"
        android:drawablePadding="8dp"
        android:layout_marginBottom="8dp" />

    <!-- Scrollable destination list -->
    <ListView
        android:id="@+id/destinationListView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="#0D000000"
        android:dividerHeight="1dp"
        android:scrollbars="vertical" />

</LinearLayout>
